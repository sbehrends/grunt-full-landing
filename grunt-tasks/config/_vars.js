/**
 * Vars
 * Set vars for usage in other tasks config
 */

module.exports = function(grunt) {
    grunt.config.set('vars', {
        app: 'app',
        assets: 'app/assets',
        dist: 'dist',
        tmp: '.tmp'
    });
};
